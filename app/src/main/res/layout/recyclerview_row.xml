<RelativeLayout
    android:layout_width="wrap_content"
    android:layout_height="@dimen/album_dimen_small"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/record_row_layout"
    android:layout_centerHorizontal="true"
>
<View
    android:id="@+id/divider"
    android:layout_width="match_parent"
    android:layout_height="1dp"
    android:background="@color/divider"
    />

    <ImageView
        android:id="@+id/record_row_image"
        android:layout_width="@dimen/album_dimen_small"
        android:layout_height="@dimen/album_dimen_small"
        android:src="@mipmap/ic_launcher"
        android:padding="8dp"
        android:layout_centerVertical="true"
        android:layout_below="@id/divider"
        />
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="bottom"
        android:layout_margin="8dp"
        android:layout_toRightOf="@id/record_row_image"
        android:layout_below="@id/divider"
        >

        <TextView
            android:id="@+id/record_row_holder_line1"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:text="Line 1"
            android:gravity="center_vertical"
            />

        <TextView
            android:id="@+id/record_row_holder_line2"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:text="Line 2"
            android:gravity="center_vertical"
            />

    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@id/record_row_image"
        android:background="@color/divider"
        />

    </RelativeLayout>